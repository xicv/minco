{
  "name": "minco-plugin-feedback-browser-tests",
  "private": true,
  "scripts": {
    "test:browser": "playwright test --config playwright.config.cjs",
    "test:browser:repeat": "playwright test --config playwright.config.cjs --repeat-each=3"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
