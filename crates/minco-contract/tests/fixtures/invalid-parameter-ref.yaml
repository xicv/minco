openapi: 3.1.0
info: {title: Invalid parameter reference, version: 1.0.0}
paths:
  /widgets:
    post:
      operationId: createWidget
      x-minco-idempotent: true
      parameters:
        - $ref: 'https://example.invalid/idempotency.yaml'
      responses:
        '201': {description: created}
        default:
          description: problem
          content:
            application/problem+json:
              schema: {type: string}
components:
  schemas: {}
