openapi: 3.1.0
info: {title: Invalid open object, version: 1.0.0}
paths:
  /health:
    get:
      operationId: getHealth
      security: []
      responses:
        '200': {description: ok}
        default:
          description: problem
          content:
            application/problem+json:
              schema: {type: string}
components:
  schemas:
    Metadata:
      type: [object, 'null']
      additionalProperties: true
