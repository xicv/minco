# minco-project-view

`minco-project-view` projects Minco's declared repository sources into a
bounded, versioned, read-only model. It preserves provenance, raw status and
independent evidence lanes without becoming another project state store.
