INSERT INTO minco_schema_metadata (key, value, updated_at)
VALUES ('{{PACKAGE}}.demo-fixture', 'enabled', CURRENT_TIMESTAMP)
ON CONFLICT (key) DO UPDATE SET
    value = EXCLUDED.value,
    updated_at = EXCLUDED.updated_at;
