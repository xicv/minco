# Seeder `{{NAME}}`

The generated source is empty and its verification query fails closed. Review the class, environment allowlist, idempotency, ownership, preservation, risk and transaction metadata before execution.
