# Migration `{{NAME}}`

Version: `{{VERSION}}`

The generated migration is intentionally empty. Add reviewed SQL, classify risk and reversibility in `.minco-migrations.toml`, then run `cargo minco db plan` and verification before apply.
