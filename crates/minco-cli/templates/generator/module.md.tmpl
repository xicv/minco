# `{{NAME}}` module

Domain invariants belong under `crates/domain`; use cases and their ports belong under `crates/application`. Delivery and infrastructure must depend inward. Generated files contain no product rules.
