//! Failing worker specification for `{{NAME}}`.
#![forbid(unsafe_code)]

#[test]
fn {{SNAKE_NAME}}_worker_behavior_must_be_implemented() {
    panic!("TODO({{NAME}}): assert worker trigger and failure semantics");
}
