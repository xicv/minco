# `{{OPERATION_ID}}`

Contract: `{{METHOD}} {{PATH}}`.

Generated specifications intentionally fail. Implement `{{PASCAL_NAME}}` in the application layer, wire exactly one use case through the HTTP handler, then replace the generated panics with authorization, validation, success, and Problem response assertions. The OpenAPI document remains authoritative.
