# Worker `{{NAME}}`

The generated worker is disabled by default and intentionally panics. Define its trigger, retry, idempotency, partial-batch, health, resource and cost behavior before enabling it.
