//! Infrastructure adapter boundary for `{{NAME}}`.
#![forbid(unsafe_code)]

#[derive(Debug, Clone, Copy)]
pub(crate) struct {{PASCAL_NAME}}Adapter;

// Implement an application-owned port here. Do not expose generic CRUD semantics.
