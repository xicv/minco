schema = 1
workspace_version = "0.3.0"
published_baseline = "0.3.0"
published_package_count = 24
adoption_gate_task = "M6-T07"

# The 0.3.0 release changes public plugin-registration APIs from 0.2.0 and
# retains the same 24-package graph.
publishable_package_count = 24

[budgets]
native_lambda_zip_max_bytes = 10485760
worker_max_batch_size = 10000
worker_max_message_bytes = 1048576
