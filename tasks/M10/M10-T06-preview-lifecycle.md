---
id: M10-T06
title: Add Verified Review Loop preview lifecycle and cleanup
milestone: M10
status: complete
priority: high
area: deployment/preview
depends_on: [M10-T02, M10-T05]
operations: []
owned_paths:
  - crates/minco-deploy-aws/**
  - crates/minco-plan/**
  - crates/minco-cli/**
  - infra/aws/**
  - docs/deployment/**
  - verification/adoption-measurements.json
  - verification/deep-review.json
  - verification/source-manifest.json
  - verification/static-validation.json
  - tasks/M10/M10-T06-preview-lifecycle.md
checks:
  - cargo test -p minco-deploy-aws -p minco-plan -p cargo-minco --all-features --locked
  - cargo minco deploy plan --environment preview
  - cargo minco destroy --environment preview --dry-run
---

## Goal

Make the Verified Review Loop the primary preview use case. Model review
identity, exact source/release/artifact digests, Feedback linkage, ownership,
expiry, cost, retained-data policy, verification, delivery trace and guarded
cleanup without introducing an implicit scheduler, hosted Minco control plane
or hidden resource deletion.

## Acceptance

- preview plans declare owner, TTL, expected account/region, resources, data
  retention, and incomplete pricing;
- a stable review ID binds the source revision, release manifest, artifact
  digests, target, verification and untrusted Feedback IDs/digests;
- review metadata and receipts remain application-owned and repository-native;
- expiry is visible but does not create a default scheduled wakeup;
- an opt-in one-time cleanup schedule uses delete-after-completion behavior and
  remains a visible `scheduled_wakeup`; manual cleanup stays supported;
- cleanup requires the exact preview identity and shows retained/deleted
  resources before apply;
- cleanup emits a receipt and verifies absence;
- production and persistent staging targets cannot use preview destroy.

## Non-goals

- unattended deletion by default;
- a global Minco-hosted review service;
- implicit environment creation from a Feedback submission;
- treating feedback content as trusted instructions;
- treating tags as sufficient deletion authority;
- preview environments with unbounded lifetime or cost.

## Review corrections

- Cleanup scheduling is Plan IR and application-owned controller metadata only.
  EventBridge Scheduler `ActionAfterCompletion=DELETE` removes the completed
  schedule, not its target environment, so no implicit controller is rendered.
- The exact provider stack inventory can contain resources generated by the
  processed template. Review creation captures those resources and their
  deletion policy instead of treating the checked-in baseline as complete.
- Repository quality refreshes deterministic deep-review and static-validation
  inventories before its exact source-manifest check. Those three generated
  verification files are therefore part of this task's bounded ownership, as
  in the preceding M10 deployment tasks.

## Completion evidence

Completed on 2026-08-03 in the isolated `minco-task-m10-t06` JJ workspace
against main parent `ee8b8887df5deee4820c761edbbe7c2096ce071e`.

- Red-green-refactor coverage proves explicit preview lifecycle/TTL/owner,
  sorted resource and retention intent, incomplete pricing, zero default
  wakeups, opt-in one-time cleanup cost visibility, invalid schedule rejection,
  immutable digest-derived review identity, untrusted Feedback references,
  generated provider-resource capture, persistent-target rejection, and
  one-way concurrency-locked cleanup receipts.
- Review manifests independently verify the exact source, release manifest,
  artifacts, successful deployment receipt, CloudFormation change-set receipt,
  account/Region/role/stack, target-config digest, verification and delivery
  evidence. Cleanup receipts independently bind that review and its exact
  delete/retain partition.
- Live review creation is read-only against AWS and fails closed on caller,
  stack, termination-protection, template, inventory, or retention ambiguity.
  Live cleanup requires the exact review digest as separate approval, uses only
  CloudFormation `STANDARD` deletion, never disables termination protection,
  and records success only after `DescribeStacks` proves stack absence.
- `cargo test -p minco-deploy-aws -p minco-plan -p cargo-minco --all-features
  --locked` passed: 67 CLI unit tests, 10 deploy CLI tests, 4 preview tests, 45
  Plan IR integration tests, and all existing deployment suites were green.
  Focused Clippy with all targets/features and warnings denied passed, as did
  `cargo fmt --all -- --check`.
- `cargo minco deploy plan --environment preview` passed with only the expected
  `MINCO-PREVIEW-006` incomplete-pricing warning and wrote the ignored preview
  plan. `cargo minco destroy --environment preview --dry-run` passed without
  AWS contact or writes and showed the disabled-target, missing-review, and
  missing-approval blockers plus seven exact baseline delete resources.
- `./scripts/quality.sh` passed static/publish/deep review, repository policy,
  SQLx isolation, browser and documentation tests, VitePress build/link checks,
  every workspace compiler/test/Clippy gate, generated PostgreSQL/SQLite app
  tests, Rustdoc, dependency/license/advisory policy, npm audit, and secret
  scanning. Its terminal source-manifest check initially reported the expected
  stale generated evidence; after refreshing the deterministic verification
  inventories, `scripts/source_manifest.py --check` passed for 721 files.
- Security review found no secret values, shell-expanded provider arguments,
  forced deletion, implicit termination-protection change, or tag-only deletion
  authority. It tightened exact target-config receipt binding, semantic review
  verification before writes, generated resource retention handling, invalid
  one-time schedule rejection, and concurrent receipt transition safety.
- Documentation now covers configuration, plan/review/destroy commands,
  Feedback digest linkage, evidence boundaries, retained-cost consequences,
  expiry semantics, application-owned scheduling, and manual fallback.

No AWS API, hosted endpoint, schedule, environment, stack, resource, release,
tag, registry, or documentation site was created, changed, or published. Live
preview creation/cleanup and release operations remain separately authorised.
