[project]
name = "minco-development"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "pyyaml>=6.0.3,<7",
]

[tool.uv]
package = false
required-version = "==0.12.3"
